"""
Shared FastAPI dependencies (DB sessions, current-user, etc.).
Import these in endpoint files via `Depends(...)`.
"""

# Example:
# from app.db.session import get_db
# def get_db_session():
#     ...
